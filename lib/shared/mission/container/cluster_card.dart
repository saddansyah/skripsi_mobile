import 'package:flutter/material.dart';

class ClusterCard extends StatelessWidget {
  const ClusterCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
